block variables

doctype html
html(lang=en)
    head
        meta(charset='urf=8')
        title #{title}
        block head

    body(class=pageClass)
        header.ui-kit-header
            include ../icon/icon_gray
        .container 
            block content

        block script
