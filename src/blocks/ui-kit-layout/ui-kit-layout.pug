block variables

include ../icons/icon-logo_gray

doctype html
html(lang=en)
    head
        meta(charset='urf=8')
        title #{title}
        block head

    body(class=pageClass)
        header.ui-kit-header
            +icon-logo-gray
            block header-content
        .container.flex 
            block content

        block script
